import 'package:flutter/material.dart';

const Color backgroundColor = Colors.white;
const Color textColor = Colors.white;
const Color textColorBlack = Colors.black;

const Color itemHourColorBG = Color(0x28CED5F1);

const Color colorBlue = Colors.blue;

const LinearGradient colorBackgroundNight = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  colors: [
    Color(0xFF080E4F),
    Color(0xFF134CB5)
  ],
);

const LinearGradient colorBackgroundDay = LinearGradient(
  begin: Alignment.centerLeft,
  end: Alignment.centerRight,
  colors: [
    Color(0xFF56A2FF),
    Color(0xFF8DC1FD)
  ],
);
